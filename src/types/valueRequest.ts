export interface confirmPassword {
    password: string;
    confirm: string;
}

export interface RegistrValue {
    email: string;
    password: string;
}

export interface Values {
    email: string;
    password: string;
    confirm: string;
    code: string;
}
